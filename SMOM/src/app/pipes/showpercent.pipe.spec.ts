import { ShowpercentPipe } from './showpercent.pipe';

describe('ShowpercentPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowpercentPipe();
    expect(pipe).toBeTruthy();
  });
});
