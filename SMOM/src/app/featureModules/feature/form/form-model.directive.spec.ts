import { FormModelDirective } from './form-model.directive';

describe('FormModelDirective', () => {
  it('should create an instance', () => {
    const directive = new FormModelDirective();
    expect(directive).toBeTruthy();
  });
});
