import { QdnAccordianDirective } from './qdn-accordian.directive';

describe('QdnAccordianDirective', () => {
  it('should create an instance', () => {
    const directive = new QdnAccordianDirective();
    expect(directive).toBeTruthy();
  });
});
